# SourceAssets

This folder is for **curated authoritative import sources** needed to reproduce Unreal assets under the Git LFS budget.

Do not place raw DAW projects, caches, every recording take, render intermediates, or disposable exports here. Use `SourceAssets/Raw/` locally/external storage for those materials; it is ignored by Git/AI indexing.
