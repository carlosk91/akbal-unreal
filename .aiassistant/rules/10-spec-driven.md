# Akbal Spec-Driven Rule

For non-trivial work, identify the approved SYS/TECH/ADR and feature package before implementation. Behavior belongs in `spec.md`, architecture in `plan.md`, execution in `tasks.md`, validation in `test-plan.md`. Stop on conflicts instead of silently choosing.
